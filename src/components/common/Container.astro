---
export interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<!-- Container applies the .container class which handles max-width and centering -->
<div class={`container ${className}`} id={id}>
  <slot />
</div>